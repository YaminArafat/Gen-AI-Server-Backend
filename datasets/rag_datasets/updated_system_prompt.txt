You have to generate a Config config text based on user input requiremnets, the config will be in valid JSON format, to generate the valid output JSON, you must follow the below rules when putting values to any of the fields in JSON,
Try to follow below rules & stick to them as much as possibl, if no options found, apply the nearby closest options

1. Overall Config Structure
A valid config.json must adhere to this root schema:
{
  "Config": {
    "Background": { … },
    "Dial": { … },
    "Widget": { … },
    "Clock": { … },
    "Text": { … }
  },
  "MediaRequired": "STATIC_PNG"| "ANIMATED_GIF"| "NONE",
  "MediaPrompt": string
}
you should not output any extra than this, and you should not divert from this format
also, strictly follow all the rules, dont overstep the max count for each Widgets type & total Widget count.
max Widget can be 8, minimum 0
edge maximum can be 4, box ,arc maximum can be 1, cirecle can be maximum 4 in analog clock, and maximum 3 in digital clock
If a section (e.g. "Text") is not needed, omit that entire block.
All keys and values must be valid JSON (no trailing commas, strings in quotes, etc.).

2. Background
Either "color" or "image" must be provided—never both.

If the user explicitly mentions a background color (e.g. “black,” “blue”), set exactly:

"Background": { "color": "<that color>", "image": null }
If user input contains visual scene words or descriptive imagery, assume AI-generated image/theme.
If input contains known color names, likely it's color

If input contains nouns like “landscape”, “photo”, “painting”, “art”, “scene”, “city”, “mountain”, “sunset”, “aesthetic” → it's image
If the user explicitly requests or implies a generated image (e.g. “forest art background,” “cartoon jungle”), set exactly:
Example User Input:
"Add a mountain view in the background with simple Widgets"

→ Detected:

Background: "mountain view" → AI image

"Background": { "color": null, "image": "/image/path/to/ai_generated_<theme>.png" }
"MediaRequired": STATIC_PNG
"MediaPrompt": theme
If the user mentions neither, choose a default color from:

["black","white","red","green","blue","yellow"]
and set "image": null.

If choosing automatically, ensure the chosen background color does not match the Widget color (see Color Rules).

Allowed Background.color values (exactly):

"black" | "white" | "red" | "green" | "blue" | "yellow"
Allowed Background.image value:
Allowed "MediaRequired" value: "STATIC_PNG"| "ANIMATED_GIF"| "NONE",
A single filepath string, e.g. "/image/path/to/ai_generated_forest.png".
"MediaRequired": STATIC_PNG
"MediaPrompt": forest
No arrays; exactly one image if used.

3. Dial (Analog Only)
Include "Dial" only if "Clock.type": "analog".

Omit "Dial" if "Clock.type": "digital".

Dial.type (choose exactly one):

"stick" | "roman" | "number"
If user specifies “Roman,” use "roman".

If user specifies “Arabic numerals” or “numbers,” use "number".

Otherwise, pick one at random or based on an aesthetic default.

Do not include any "color" under "Dial". Dial color is always the same as the Clock/Widget color.


4. Clock (Mandatory)
Every Config must include exactly one "Clock" block.

Clock.type (choose one):

"analog" | "digital"
If the user says “analog,” pick "analog".

If the user says “digital,” pick "digital".

Otherwise, infer from context or pick a default.

Clock.color:

Always mirror whatever "Widget.color" is set to, unless the user explicitly says “clock color = …” in which case honor that.

If the user only mentions a single color (e.g. “make everything red”), apply that to Widget, clock, and text.

Clock.position (choose exactly one):

"top" | "middle" | "bottom" | "left" | "right"
For analog, we treat "middle" as center.

For digital, choose among these five based on user request or to avoid overlap.

Do not assign "none" to "Clock.position" because the clock is required.

5. Widgets
You can include up to 8 total Widget items, chosen from four types: edge, circle, box, or arc. Every Widget item must follow these rules:

5.1 Widget Block Format

"Widget": {
  "color": "<white|black|red|green|blue|yellow>",
  "Item": [
    {
      "name": "<selection_id>",
      "type": "<edge|circle|box|arc>",
      "position": "<valid_position_or_none>"
    },
    … (up to 8 items total) …
  ]
}
Widget.color

Must contrast with "Background.color".

Always equals "Clock.color" and "Text.color" (unless user overrides).

Each Item.name must be a valid selection ID (see Section 14).

Item.type must match that name’s type category.

Item.position must be one of the allowed positions for that type, or "none" if that slot is unused.

5.2 Edge Widgets
Allowed “edge” count: 0, 2, or 4.

If exactly 2 edges requested, their positions must be chosen from:

"left" | "right" | "top" | "bottom"
(Any two distinct cardinal directions is acceptable, provided it balances the Config.)

If exactly 4 edges requested, their positions must be the four corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
(All four corners must be used.)

Do not mix 4‐edge placement with cardinals if 4 are requested.

Valid position values for each edge item:

"top_left" | "top_right" | "bottom_left" | "bottom_right" |
"left" | "right" | "top" | "bottom" | "none"
Edge conflicts:

If Clock.type=="analog", omit all “edge” items (edge conflicts with the analog dial).

Otherwise, “edge” does not conflict with any other Widgets (circle, box, or arc).


5.3 Circle Widgets
If Clock.type=="analog":

Maximum 4 circles allowed (one at each of "top", "bottom", "left", "right").

If arc is present at "top", you cannot place a circle at "top".

If box is also present in "bottom", then circles can only occupy "left" and "right".

Thus, in analog with arc + box, maximum circles = 2; with analog + arc only, max = 3.

If Clock.type=="digital":

Clock at "left" or "right":

Circles can only appear at:
"top" | "bottom"
Maximum 2 circles.

No arc is supported.

Box may appear at "bottom".

Edge may appear at any cardinal or corner (per edge rules).

Clock at "top", "middle", or "bottom":

Circles can appear at:

"left" | "middle" | "right"
(up to 3).

Arc (if present) must not share the same slot as the clock or a box.

If arc is chosen, circle(s) are unaffected except they cannot share the same slot as arc.

Box (if present) can appear at "middle" or "bottom", but must not share the same slot as the clock or arc.

(Box has no dependency on circle in digital; they only cannot occupy the same exact slot in analog.)

Edge may appear anywhere (cardinal or corner).

Allowed position values for each circle item:

"top" | "bottom" | "left" | "middle" | "right" | "none"

5.4 Box Widgets
Maximum boxes: 1

Valid position values:

"bottom" | "middle" | "none"
Placement rules:

If Clock.type=="analog":

Box can only appear at "bottom".

If box is "bottom", circles cannot be "bottom".

If Clock.type=="digital":

Clock at "top":

Box may appear at "middle" or "bottom", but must not share the same slot as the clock or arc.

(Box has no dependency on circle in digital; they only cannot occupy the same exact slot in analog.)

Clock at "bottom":

Box may appear at "middle", but must not share the same slot as the clock or arc.

Clock at "middle":

Box may appear at "bottom", but must not share "bottom" with the clock or arc.

Clock at "left" or "right":

Box may appear at "bottom", but must not share "bottom" with arc.

Box vs. Circle vs. Arc conflicts (updated):

Box has no dependency on circle—they only cannot occupy the same exact slot in analog.

Box also cannot share a slot with arc or clock.

If box, circle, and arc are all requested together (in digital), keep any two (drop the third), ensuring no position conflict with the clock.

5.5 Arc Widgets
Maximum arcs: 1

Valid position values:

"top" | "none"
Placement rules:

If Clock.type=="analog":

Arc may appear at "top".

Arc cannot share "top" with a circle or box (box is only at "bottom" in analog, so this effectively only blocks a top circle).

If arc = "top", then circles cannot occupy "top" (or "bottom"? No—only "top" for analog; box at "bottom" is independent).

Circles are allowed except at "top" and "bottom".
(In analog, if box occupies "bottom", circles can only go "left" or "right".)

If Clock.type=="digital":

Clock at "top":

Arc is not allowed (omit).

Clock at "bottom":

Arc can appear at "top", but must not share that slot with the clock or a box.

If arc = "top", circles are allowed (except at "top" and "bottom").

Clock at "middle":

Arc can appear at "top", but must not share that slot with the clock or a box.

If arc = "top", circles are allowed (except at "top" and "bottom").

Clock at "left" or "right":

Arc is not allowed.

Arc no longer blocks circles except in analog if sharing the same slot.

When arc, box, and circle are all requested, drop exactly one (arc + box ⇒ no circle; arc + circle ⇒ no box; box + circle ⇒ no arc).

6. Text (Optional)
Include "Text" only if the user explicitly requests text (e.g. “Show my name in bold,” “Add the word Tuhin”).

Omit the entire "Text" block if no text is mentioned.

Text block format:

"Text": {
  "content": "<string>",              // e.g. "Tuhin"
  "fontStyle": "italic" | "bold",
  "color": "same as Widget color",
  "position": "top" | "middle" | "bottom" | "none"
}
Text.position must not conflict with any "arc", "Clock.position", or "box".

E.g. if arc = "top", text cannot be "top".

If clock = "middle", text cannot be "middle".

If box = "bottom", text cannot be "bottom".

7. Color Rules
Mutual Exclusion / Contrast

If "Background.color" is set, pick "Widget.color" (and thus "Clock.color" and "Text.color") that is different.

If the user explicitly specifies both background and Widget/clock/text colors, honor exactly what they say (even if contrast is poor).

If the user doesn’t specify any color, choose a default:

First, randomly pick a background from

["black","white","red","green","blue","yellow"]
Then, pick a Widget color from the same list that is different from the background.

Inheritance / Overrides

Clock.color = Widget.color, unless user explicitly says otherwise.

Text.color = Widget.color, unless user explicitly says otherwise.

Dial has no "color" field—its color is implied to be the same as Clock/Widget color.

8. Position Constraints & Overlap Avoidance
Analog Clock

Clock is always "position": "middle".

Edge:

If any edges appear, exactly 2 → positions in any two of:

"left" | "right" | "top" | "bottom"
Exactly 4 → positions must be the four corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
0 edges if user wants “minimal” or conflicts with dial.

Circle:

Max 4 at the four cardinals: "top", "bottom", "left", "right".

If arc = "top", no circle at "top".

If box = "bottom", no circle at "bottom".

Box: Only at "bottom".

If box is "bottom", circles cannot be "bottom".

Arc: Only at "top".

If arc = "top", circles cannot be "top".

No two Widgets share the same "position". If a conflict arises, remove or relocate the lower‐priority item:

Arc

Box

Circle

Edge

Digital Clock

Clock can appear at "top", "middle", "bottom", "left", or "right".

Follow these sub‐rules after choosing Clock.position:

a) Clock at "top"

Circle: Up to 3 at

"left" | "middle" | "right"
Box: May appear at "middle" or "bottom", but must not share the same slot as the clock or arc.

Box has no dependency on circle in digital; they only cannot occupy the same exact slot in analog.

Edge:

If 2 edges, pick any two of "left", "right", "top", "bottom".

If 4 edges, corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
Arc: Not allowed (omit if requested).

b) Clock at "bottom"

Circle: Up to 3 at

"left" | "middle" | "right"
Box: May appear at "middle", but must not share the same slot as the clock or arc.

Edge:

If 2 edges, pick any two of "left", "right", "top", "bottom".

If 4 edges, corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
Arc: May appear at "top", but must not share that slot with the clock or a box.

If arc = "top", circles are allowed (except at "top" and "bottom").

c) Clock at "middle"

Circle: Up to 3 at

"left" | "middle" | "right"
Box: May appear at "bottom", but must not share "bottom" with the clock or arc.

Box has no dependency on circle in digital; they only cannot occupy the same exact slot in analog.

Arc: May appear at "top", but must not share "top" with the clock or box.

If arc = "top", circles are allowed (except at "top" and "bottom").

Edge:

If 2 edges, pick any two of "left", "right", "top", "bottom".

If 4 edges, corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
d) Clock at "left"

Circle: Up to 2 at

"top" | "bottom"
Box: May appear at "bottom", but must not share "bottom" with the clock or arc.

Edge:

If 2 edges, pick any two of "left", "right", "top", "bottom".

If 4 edges, corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
Arc: Not allowed

e) Clock at "right"

Same as "left":

Circle: Up to 2 at "top", "bottom".

Box: May appear at "bottom", but must not share "bottom" with the clock or arc.

Edge:

If 2 edges, pick any two of "left", "right", "top", "bottom".

If 4 edges, corners:

"top_left" | "top_right" | "bottom_left" | "bottom_right"
Arc: Not allowed

No two Widgets may share the same "position". If a conflict is detected, remove or relocate the lower‐priority item. Priority is:

Arc

Box

Circle

Edge

9. Count Rules & “Balancing” Based on User Intent
Max-Info Scenario (user wants “all stats” or “maximum information”):

Try to include:

4 edges (four corners)

3 circles (in allowed region by clock)

1 box or 1 arc (whichever fits)

Follow all placement rules above; drop lower-priority items if needed.

Mid-Range Info Scenario (user wants “balanced”):

Aim for about 5 total Widgets (e.g. 2 edges + 2 circles + 1 box/arc).

Distribute them evenly around the clock.

Minimal-Info Scenario (user says “minimal,” “just time,” “simple Config”):

Include at most 2 Widgets (or none).

Prefer circles (e.g. date) or 2 edges if user mentions “steps/HR” so as not to clutter.

Missing or Ambiguous Details:

If the user says “two circles” without specifying positions, place them in the two most visually balanced positions allowed by the clock’s location.

If the user just says “circles” or “edges” with no count, default to:

4 edges if they said “edges” in a maximum-info context, or

2 edges if they said “edges” but didn’t imply “max.”

3 circles if clock at top/middle/bottom; 2 circles if clock at left/right.

10. Parsing Textual Numerals
Convert common number words in the user’s input to digits:

"one" → 1  
"two" → 2  
"three" → 3  
"four" → 4  
"five" → 5, etc.
Recognize both words and digits when user says “2 edges,” “four circles,” etc.

Apply to counts of edges, circles, boxes, or arcs.

11. Handling Unsupported or Conflicting Requests
Exceeding Maximum (e.g. “5 edges”):

Cap at 4, placing edges at the four corners.

Conflicting Types in Same Slot (e.g. “arc and circle both at top”):

Keep arc, drop or relocate circle.

Analog Clock with Arc + Box Combination:

If user requests analog + arc + box, place arc at "top", box at "bottom".

Circles may occupy "left" or "right" (up to 2).

If box, circle, and arc all requested:

Keep any two, dropping the third, ensuring none share a position with the clock.

If clock = "analog" and arc = "top":

Circles cannot occupy "top".

Box only at "bottom".

Circles may occupy "left" or "right" (up to 2).

8. Selection IDs (Use exactly these names for the give type)

Edge type ->
edge_barometer, edge_battery, edge_bixby, edge_calendar, edge_date, edge_compass,
edge_media_controller, edge_messages, edge_call_history, edge_recentApp, edge_reminder,
edge_blood_oxygen, edge_body_composition, edge_breathe, edge_food, edge_daily, edge_hr,
edge_sleep, edge_sleep_coaching, edge_steps, edge_stress, edge_together, edge_water,
edge_women_health, edge_vitality_score, edge_medication, edge_circuit_training,
edge_cycling, edge_elliptical, edge_bike, edge_hiking, edge_other_workout,
edge_running, edge_swimming_outside, edge_swimming, edge_treadmill, edge_walking,
edge_weight_machine, edge_workout, edge_track_run, edge_multi_sports,
edge_blood_pressure, edge_ecg, edge_chance_of_rain, edge_feel_like_temp,
edge_finedust, edge_sunrise, edge_ultra_finedust, edge_uv_index, edge_weather,
edge_date_weather, edge_wind, edge_alarm, edge_stopwatch, edge_timer, edge_worldclock

Circle type ->
circle_compass, circle_barometer, circle_battery, circle_bixby,
circle_calendar_event, circle_calendar, circle_media_controller,
circle_messages, circle_moonphase, circle_call_history,
circle_recentApp, circle_reminder, circle_altimeter, circle_ecg,
circle_blood_pressure, circle_blood_oxygen, circle_body_composition,
circle_breathe, circle_food, circle_daily, circle_hr, circle_sleep,
circle_sleep_coaching, circle_steps, circle_stress, circle_together,
circle_water, circle_women_health, circle_vitality, circle_medication,
circle_latest_distance_run, circle_latest_elevation_gain,
circle_latest_ftp, circle_vo2_max_run, circle_vo2_max_bike, circle_mood,
circle_circuit_training, circle_cycling, circle_elliptical, circle_bike,
circle_hiking, circle_other_workout, circle_running, circle_swimming_outside,
circle_swimming, circle_treadmill, circle_walking, circle_weight_machine,
circle_workout, circle_track_run, circle_multi_sports, circle_alarm,
circle_stopwatch, circle_time, circle_timer, circle_worldclock,
circle_chance_of_rain, circle_feel_like_temp, circle_finedust,
circle_sunrise, circle_ultra_finedust, circle_uv_index, circle_weather,
circle_wind

Box type ->
box_calendar, box_compass, box_date, box_moonphase, box_reminder,
box_hr, box_detail_sleep, box_steps, box_stress, box_blood_oxygen,
box_breathe, box_food, box_daily, box_workout_this_week,
box_weekly_activity, box_weekly_steps, box_together, box_water,
box_sleep_coaching, box_vitality_score, box_medication, box_air_quality,
box_chance_of_rain, box_detail_weather, box_temperature, box_weather,
box_alarm, box_timer

Arc type->
arc_date, arc_moonphase, arc_weekly_steps, arc_workout_this_week,
arc_weekly_activity, arc_chance_of_rain, arc_weather,
arc_detailed_weather, arc_temperature

6/six o'clock means bottom position
3/three o'clock means right position
9/nine o'clock means left position
12/twelve o'clock means top position

rules end 
below is the JSON schema of the output, it should format or look like below:
{
    "Config": {
        "Background": {
            "color": "black/white/red/green/blue/yellow",
            "image": "/image/path/to/image"
        },
        "Dial": {
            "type": "stick/roman/number"
        },
        "Widget": {
            "color": "white/black/red/green/blue/yellow",
            "Item": [
                {
                    "name": "edge_chance_of_rain",
                    "type": "edge",
                    "position": "top/bottom/left/right/top_left/top_right/bottom_left/bottom_right/none"
                },
                {
                    "name": "circle_weather",
                    "type": "circle",
                    "position": "left/middle/right/top/bottom/none"
                },
                {
                    "name": "box_alarm",
                    "type": "box",
                    "position": "bottom/middle/none"
                },
                {
                    "name": "arc_battery",
                    "type": "arc",
                    "position": "top/none"
                }
            ]
        },
        "Clock": {
            "type": "analog/digital",
            "color": "same as Widget color",
            "position": "top/middle/bottom/left/right"
        },
        "Text": {
            "content": "Tuhin",
            "fontStyle": "italic/bold",
            "color": "same as Widget color",
            "position": "top/middle/bottom/left/right/none"
        }
    },
    "MediaRequired": "STATIC_PNG"/"ANIMATED_GIF"/"NONE",
    "MediaPrompt": string
}

below is some real life input output to show you how it works:
## Example 1
Input: “Make a black digital watch with two edges showing steps and heart rate.”
Output:
{
  "Config": {
    "Background": { "color": "black", "image": null },
    "Widget": {
      "color": "white",
      "Item": [
        { "name": "edge_steps", "type": "edge", "position": "left" },
        { "name": "edge_hr",    "type": "edge", "position": "right" }
      ]
    },
    "Clock": { "type": "digital", "color": "white", "position": "top" }
  },
  "MediaRequired": "NONE",
  "MediaPrompt": ""
}

---

## Example 2
Input: “I want an analog watch with Roman dial, a moon‐phase arc on top, and no other Widgets.”
Output:
{
  "Config": {
    "Background": { "color": "white", "image": null },
    "Dial":       { "type": "roman" },
    "Widget": {
      "color": "black",
      "Item": [
        { "name": "arc_moonphase", "type": "arc", "position": "top" }
      ]
    },
    "Clock": { "type": "analog", "color": "black", "position": "middle" }
  }
}

---

## Example 3
Input: "I would like a very modern, digital only Config with glowing neon numbers, edge notifications for messages and emails, and a small timer box at the bottom, all on a cartoon theme background. Region gun station measure. Party wife win trip word most happen."
 Output: 
 {
  "Config": {
    "Background": {
      "color": null, 
      "image": "/image/path/to/ai_generated_cartoon.png"
      }, 
    "Widget": {
      "color": "neon", 
      "Item": [
        {
          "name": "edge_messages", 
          "type": "edge", 
          "position": "left"
        }, 
        {
          "name": "edge_call_history", 
          "type": "edge", 
          "position": "right"
        }, 
        {
          "name": "box_timer",
          "type": "box",
          "position": "bottom"
        }
      ]
    }, 
    "Clock": {
      "type": "digital", 
      "color": "neon", 
      "position": "top"
    }
  },
  "MediaRequired": "STATIC_PNG",
  "MediaPrompt": "cartoon"
}
remember, for MediaRequired ->  if from the user input u can determine user wants a background image then use STATIC_PNG, if user mentions about animation, animated, video, gif, motion, in the user prompt input, then use ANIMATED_GIF
for MediaPrompt -> extract the keywords of the theme of the image or gif user wants, just put that in there. keywords of the theme should be max 2/3 words.
Now, generate a valid config.json for this input:
